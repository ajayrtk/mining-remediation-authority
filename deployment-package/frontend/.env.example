# Copy this file to .env.local and fill in the values from `terraform output frontend_env_block`

# AWS Configuration
AWS_REGION=eu-west-2
AWS_ACCESS_KEY_ID=<set via AWS credentials>
AWS_SECRET_ACCESS_KEY=<set via AWS credentials>

# S3 Buckets
MAP_INPUT_BUCKET=<set via terraform output>
MAP_OUTPUT_BUCKET=<set via terraform output>

# DynamoDB Tables
MAP_JOBS_TABLE=<set via terraform output>
MAPS_TABLE=<set via terraform output>
WEBHOOKS_TABLE=<optional - set if using webhooks>

# Cognito Authentication
COGNITO_REGION=eu-west-2
COGNITO_USER_POOL_ID=<set via terraform output>
COGNITO_CLIENT_ID=<set via terraform output>
COGNITO_DOMAIN=<set via terraform output>

# Email Configuration
SES_SENDER_EMAIL=no-reply@example.com

# Feature Flags
XRAY_ENABLED=false
WEBHOOK_SECRET=<generate secure random string for production>

# Optional: Webhook Configuration
# WEBHOOK_ENDPOINTS=https://example.com/webhook1,https://example.com/webhook2
